# Pulse Reports

Product app built **with** the `@demo/shadcn-ui` design-system package (shadcn-shaped).

```bash
# From Decree repo root
cd demos/shadcn/apps/app
npm install
npm run dev   # http://127.0.0.1:5180
```

Inherits Decree contract from `../packages/ui` via `decree use`.
