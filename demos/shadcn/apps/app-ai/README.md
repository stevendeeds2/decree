# Pulse Reports AI

Product-shaped twin of Pulse Reports, rebuilt by an AI agent **from a screenshot only**.

Does **not** depend on `@demo/shadcn-ui`. Amber banner in the UI states provenance.

```bash
# From Decree repo root
cd demos/shadcn/apps/app-ai
npm install
npm run dev   # http://127.0.0.1:5181
```

For Decree A/B, copy the Pulse Reports contract and verify (expect fail).
