# Pulse Reports

Product app built **with** the `@pulse/ui` design-system package (shadcn-shaped).

```bash
# From Decree repo root
cd demos/pulse-reports/apps/pulse-reports
npm install
npm run dev   # http://127.0.0.1:5180
```

Inherits Decree contract from `../packages/pulse-ui` via `decree use`.
