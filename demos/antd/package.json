{
  "name": "antd-demo",
  "private": true,
  "version": "1.0.0",
  "description": "Decree demo — Ant Design + compliant app + AI twin",
  "scripts": {
    "postinstall": "npm install --prefix packages/ui --omit=peer && npm install --prefix apps/app && npm install --prefix apps/app-ai",
    "dev": "npm run dev --prefix apps/app",
    "dev:ai": "npm run dev --prefix apps/app-ai"
  }
}
